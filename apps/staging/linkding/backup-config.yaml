apiVersion: v1
kind: ConfigMap
metadata:
  name: backup-config
data:
  run-backup.bash: |
    #!/bin/bash
    set -euo pipefail

    backup_dir="${BACKUP_DIR:?}/linkding"
    backup_file="backup_$(date '+%Y-%m-%d_%H-%M-%S').zip"

    mkdir -p "${backup_dir}"

    tmp_file="${backup_dir}/.${backup_file}.partial"
    trap 'rm -f "${tmp_file}"' EXIT

    python manage.py full_backup "${tmp_file}"
    mv "${tmp_file}" "${backup_dir}/${backup_file}"

    echo "Backup written to ${backup_dir}/${backup_file}"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sftp-known-hosts
data:
  known_hosts: |
    sftp-svc.backups.svc.cluster.local ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFb9bxTA3lP9JZzc25ucJIBiQedSwxzz0GNqKWyQoNJZ sftp-host
